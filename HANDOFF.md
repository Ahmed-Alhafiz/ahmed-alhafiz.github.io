# HANDOFF — الحالة الحية للموقع

آخر تحديث: 2026-09-23 22:23 Europe/Vienna

## الحالة الحالية

- المستودع: `Ahmed-Alhafiz/ahmed-alhafiz.github.io`، ومصدر الحقيقة الدائم هو GitHub.
- الفرع الجاهز للنشر: `content/umm-abbas-multilingual-discovery`.
- رأس التنفيذ المحلي: `34e6deb95e12d3413e689fa9439ba1b556151893`، مبني على `main` عند `b6b5cd847579e14b32637f6de0b6bdd4e3f5e15f`.
- الحالة: التنفيذ والفحوص المحلية مكتملان؛ الرفع وPR والدمج والنشر والمتابعة الخارجية مؤجلة إلى اللابتوب المخوّل.
- لا تغيّر DNS أو إعدادات الدومين: الدومين الرسمي وHTTPS وتحويل `www` والرابط القديم كانت تعمل في آخر تحقق.

## ما تم فعليًا

- دليل «أم عباس» كامل بالعربية والإنجليزية والألمانية حول تداخل الأعراض المنسوبة للمس مع الاضطرابات النفسية أو العصبية، بصياغة تحترم الإطار الديني ولا تحول المقال إلى تشخيص طبي.
- روابط `hreflang` متبادلة ومبدّل لغة ظاهر بين النسخ الثلاث.
- ربط الدليل بصفحات الكتاب المحلية، والصفحات الرئيسية المناسبة، ومراكز الأبحاث العربية والإنجليزية والألمانية.
- مركز أبحاث ألماني فعلي عند `/de/articles/`، مع إدراجه في التنقل وSitemap.
- جواب بحثي مباشر عن التشنج أو الصراخ أو التأثر أثناء الرقية: رد الفعل وحده لا يثبت تشخيصًا، مع منع الإكراه أو تأخير الرعاية.
- ثمانية مصادر ظاهرة ومضمّنة في `Article.citation` لكل نسخة لغوية.
- ربط كياني في `author.json`: كتاب «أم عباس» ← الدليل العربي ← الترجمتان الإنجليزية والألمانية، مع مؤلف واحد وهوية واحدة.
- اختبارات آلية تمنع كسر الاقتباسات أو الترجمة أو روابط الكتاب والكاتب مستقبلًا.
- المخزون المقترح بعد النشر: 52 صفحة عامة، 52 canonical، و52 رابطًا في Sitemap؛ 10 مواد بحثية، 10 عناصر عربية و5 عناصر إنجليزية في الخلاصات.

## ما يجري العمل عليه الآن

- لا يوجد تعديل محتوى محلي مفتوح بعد هذه الحزمة.
- العمل المتبقي هو مسار النشر الخارجي الموحد على اللابتوب، ثم القياس بعد إعادة الزحف.

## التحقق المنجز

- `site_audit.py`: 52 صفحة، 52 canonical، 52 Sitemap، صفر أخطاء وصفر تحذيرات.
- `discovery_integrity.py`: 10 أسطح عربية، 5 إنجليزية، ونسخة ألمانية؛ الترابط الزمني واللغوي والكتابي متسق.
- `entity_integrity.py`: نجح مخطط هوية الكاتب ومخطط «أم عباس» الثلاثي.
- `visibility_audit.py`: البنية المحلية ناجحة؛ النتائج الخارجية غير مقاسة.
- JSON و`git diff --check`: ناجحان.
- الفحص البصري المحلي غير متاح لغياب Chromium؛ لذلك فحص `Visual review / render` في PR شرط دمج لا يُتجاوز.

## ما لم يُفحص / حدود الدليل

- لم يُنشر هذا الفرع بعد، لذلك لا يُدّعى أن الصفحات الجديدة حية أو مفهرسة.
- لا يوجد ضمان للمركز الأول ولا دليل بعد على ترتيب أو زيارات أو استشهادات من وكلاء الذكاء الاصطناعي.
- تنبيه `ProfilePage.dateModified` التاريخي في Search Console لا يُعتبر مختفيًا حتى تعيد Google الزحف وتظهر بيانات حديثة.

## المخاطر أو الـblockers

- هذه البيئة لا تملك اعتماد GitHub مصرحًا للرفع؛ لذلك لا يمكن دفع الفرع أو فتح PR منها.
- غياب Chromium محليًا يجعل نجاح `Visual review / render` على GitHub شرطًا إلزاميًا قبل الدمج.
- نشر كمية كبيرة من صفحات ضعيفة أو عامة قد يخفف تركيز الموقع؛ لا توسع المحتوى خارج صلته المباشرة بالمؤلفات.

## الخطوة التالية الدقيقة

1. اقرأ `PROJECT_STATE.json` وهذا الملف، وتأكد أن المصدر هو الفرع والرأس المذكوران أعلاه. إذا لم يكن الفرع موجودًا على الجهاز، استورد ملف الـbundle المرفق بدل إعادة العمل يدويًا.
2. ارفع الفرع نفسه إلى GitHub من دون force-push ومن دون تعديل `main` مباشرة.
3. افتح Pull Request واحدًا مكتمل حقول التوثيق، يشمل كل commits بعد رأس الأساس المذكور.
4. انتظر الفحوص الستة التالية:
   - `PR policy / policy`
   - `Governance integrity / validate`
   - `Citation metadata integrity / validate`
   - `Content research architecture / validate`
   - `Site integrity / audit`
   - `Visual review / render`
5. إذا فشل فحص، أصلحه على الفرع نفسه وأعد الفحوص. لا تدمج قبل نجاح الستة.
6. ادمج PR، وانتظر نجاح نشر GitHub Pages ومهام `main`.
7. افحص حيًا النسخ العربية والإنجليزية والألمانية، و`/de/articles/`: HTTP 200، canonical، `hreflang`، مبدّل اللغة، المراجع، الصور، الهاتف وسطح المكتب، وعدم وجود تمرير أفقي. تحقق أن `sitemap.xml` يعرض 52 URL.
8. في Google Search Console: أرسل `sitemap.xml` مرة واحدة، ثم اطلب الفهرسة للنسخ الثلاث ولـ`/de/articles/`. لا تكرر الطلب بلا داعٍ.
9. بعد إعادة الزحف، راجع Page indexing وأداء الاستعلامات العربية والإنجليزية والألمانية وتنبيه ProfilePage القديم، ثم سجّل النتائج المؤرخة في `PROJECT_STATE.json` و`HANDOFF.md`.

### حدود المهمة على اللابتوب

- لا تغيّر DNS أو خدمة الاستضافة أو الدومين أو بنية GitHub Pages.
- لا تضف صفحات عامة بعيدة عن المؤلفات ولا روابط أو مراجعات أو حسابات خارجية زائفة.
- لا تكرر المحتوى ولا تنشئ doorway pages لأسماء الكاتب.
- لا تعتبر نجاح الإرسال إلى Search Console إثباتًا للفهرسة أو الترتيب.

## ملفات أو بيانات محلية لا تنتقل عبر Git

- حزمة Git قابلة للنقل: `/workspace/scratch/cce0faa09f10/umm-abbas-multilingual-discovery.bundle`.

## فحص الاستلام على الجهاز الآخر

النتيجة الصحيحة بعد التنفيذ: الفرع مرفوع، PR مدموج بعد ستة فحوص ناجحة، Pages منشور، الصفحات الأربع المستهدفة مفحوصة حيًا، Sitemap يحوي 52 رابطًا، وSearch Console استلم الخريطة وطلبات الفهرسة. عندها فقط حدّث الحالة إلى `deployed_monitoring`.
