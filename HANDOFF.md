# HANDOFF — الحالة الحية للموقع

آخر تحديث: 2026-09-24 21:10 Europe/Vienna

## الحالة الحالية

- الحالة: `deployed_monitoring`.
- نُشر دليل «أم عباس» متعدد اللغات عبر [PR #65](https://github.com/Ahmed-Alhafiz/ahmed-alhafiz.github.io/pull/65).
- أحدث `origin/main` المؤكد قبل إصلاح البيانات المنظمة: `588e1ad783e340f66e430a743d1fbdbf01f7c67c`.
- commit إصلاح البيانات المنظمة: `7ef37c350f6dc906c11af573f0868233e59fc26d`.
- لا تغيّر DNS أو GitHub Pages أو الدومين؛ الموقع الرسمي وHTTPS يعملان.

## ما تم فعليًا

- نجحت الفحوص الستة المطلوبة على PR #65: PR policy، Governance integrity، Citation metadata integrity، Content research architecture، Site integrity، وVisual review.
- اكتمل نشر GitHub Pages بنجاح في workflow رقم `36028817990`.
- عادت الصفحات التالية بحالة HTTP 200 عبر HTTPS:
  - `/articles/possession-or-neurological-psychological-disorder/`
  - `/en/articles/possession-or-neurological-psychological-disorder/`
  - `/de/articles/possession-or-neurological-psychological-disorder/`
  - `/de/articles/`
  - `/sitemap.xml`
- تطابقت canonical وhreflang المتبادلة ومبدّلات اللغات في النسخ الثلاث.
- كل نسخة من الدليل تعرض 8 مراجع و3 روابط لغات، وصورة الشعار الحية أعادت HTTP 200.
- اجتاز الفحص البصري النسخ الثلاث عند سطح المكتب `1440x1200` والهاتف `390x844` والهاتف الضيق `360x800`، بما في ذلك منع التمرير الأفقي.
- Sitemap الحية تضم 52 رابطًا.

## ما يجري العمل عليه الآن

- إصلاح metadata فقط: استبدال ثمانية تعريفات `Dataset` غير مكتملة بـ`CreativeWork` لأنها سجلات أدلة مؤلفة، وليست Dataset منشورة بترخيص وجهة نشر مؤسسية معلنين.
- لا يوجد تغيير في النصوص المرئية أو الأفكار أو التصميم أو DNS أو GitHub Pages.
- بعد النشر تبدأ عملية Validate Fix مرة واحدة لتقريري Dataset وProfilePage، ثم يصبح العمل انتظار بيانات Google الجديدة.

## التحقق المنجز

- نجحت فحوص الموقع والاكتشاف والكيان والـJSON والفرق محليًا.
- نجح `python -X utf8 tools/visibility_audit.py --live` بعد التصحيح، بالحالتين المحلية والحية ومن دون أخطاء.
- نجح النشر والفحص الحي وVisual review كما هو موثق أعلاه.
- نجح التدقيق المستهدف الجديد: `Site integrity` يعرض 52 صفحة و52 canonical و52 رابط Sitemap وصفر أخطاء أو تحذيرات؛ `Entity integrity` ناجح؛ لا توجد عقد `Dataset` متبقية؛ وقيم `ProfilePage.dateModified` الست كلها ISO 8601 DateTime صحيحة.

## Search Console

- الخاصية المستخدمة: `sc-domain:ahmedalhafiz.com`.
- أُعيد إرسال `https://ahmedalhafiz.com/sitemap.xml` مرة واحدة في 2026-09-24، وظهر نجاحها مع 52 صفحة مكتشفة.
- قُبلت طلبات الفهرسة الأربعة في قائمة انتظار الزحف ذات الأولوية:
  - النسخة العربية: كانت تظهر بالفعل على Google قبل الطلب.
  - النسخة الإنجليزية: كانت «تم اكتشاف الصفحة - لم تتم فهرستها حتى الآن».
  - النسخة الألمانية: لم يكن Google قد تعرّف إلى URL بعد.
  - `/de/articles/`: لم يكن Google قد تعرّف إلى URL بعد.
- قبول الطلب ليس إثباتًا للفهرسة أو الترتيب، ولا يجوز تكرار الطلبات بلا سبب جديد.
- تقرير Dataset بتاريخ 2026-09-24 كان يعرض عنصرين صالحين و4 غير صالحة. السبب الحرج هو `description` المفقود في 4 تعريفات مختصرة، مع توصيات `license` و`creator` و`publisher` على 6 عناصر. لم يُختلق ترخيص أو ناشر؛ أزيل نوع Dataset غير المنطبق.
- تقرير ProfilePage يعرض 4 عناصر صالحة وصفر غير صالح، لكنه ما زال يحتفظ بتحذير تاريخي لـ4 عناصر حول `dateModified`. النسخة الحية نفسها صحيحة وتحتاج Validate Fix ثم إعادة زحف Google.

## Apps Script

- المشروع الموجود نفسه يحتوي `runDailyGscReport` وعامل تشغيل يومي واحد؛ لم يُنشأ مشروع أو Trigger إضافي.
- صُححت `SITE` من `https://ahmed-alhafiz.github.io/` إلى `sc-domain:ahmedalhafiz.com` وحُفظت في Drive.
- بدأت محاولة اختبار واحدة فقط. طلب Google إعادة تفويض OAuth، لكن نافذة الموافقة المنبثقة لم تظهر داخل المتصفح الآمن؛ أُلغيت المحاولة قبل تنفيذ الدالة. لا يجوز تكرار الاختبار قبل إكمال التفويض.

## GitHub Billing

- القراءة في 2026-09-24 أظهرت أن الاستهلاك الحالي أصبح `$9.61` وليس `$9.03` من ميزانية Actions الشهرية.
- المصدر حسب SKU: Linux = 4,941 دقيقة، `$29.65` إجمالي و`$9.00` مدفوع؛ التخزين = 4,907.59 GB-hour، `$1.65` إجمالي و`$0.61` مدفوع؛ macOS 3-core = 63 دقيقة و`$0` مدفوع.
- المصدر حسب المستودع: `rahma-prayer` نحو `$7.19` مدفوع، و`thabatak-app` نحو `$2.43`؛ أما `ahmed-alhafiz.github.io` فبلغ `$12.63` إجمالي لكن `$0` مدفوع. فرق سنت واحد ناتج عن تقريب صفوف GitHub.
- لم تُرفع الميزانية ولم تُغيّر أي إعدادات مالية.

## تصحيح مراقب الظهور

- فشل `Visibility readiness monitor` بعد الدمج لأن أداة الفحص كانت تتوقع رقمًا ثابتًا قديمًا هو 9 عناصر بحث، بينما المخزون المنشور الصحيح هو 10.
- التصحيح الصغير يستمد العدد المتوقع من `data/content-inventory.json` بدل الرقم الثابت.
- بعد التصحيح نجح `python -X utf8 tools/visibility_audit.py --live`: الحالة المحلية والحية ناجحتان، ولا أخطاء.

## ما لم يُفحص / حدود الدليل

- لا يوجد ادعاء بأن النسخ الإنجليزية أو الألمانية فُهرست بعد قبول الطلب.
- لا يوجد دليل حالي على ترتيب أو انطباعات أو زيارات أو استشهادات خارجية جديدة.
- لا يُعتبر تحذيرا Dataset و`ProfilePage.dateModified` مختفيين قبل نجاح التحقق وإعادة زحف Google.
- اختبار Apps Script لم يكتمل بسبب نافذة OAuth المنبثقة غير المتاحة للمتصفح الآمن.

## المخاطر أو الـblockers

- العائق الوحيد المباشر هو إكمال موافقة OAuth الخاصة بـApps Script ثم تشغيل `runDailyGscReport` مرة واحدة.
- الخطر المتبقي زمني وخارجي: قرارات الزحف والفهرسة والترتيب تخص Google ولا يثبتها نجاح الإرسال.

## الخطوة التالية الدقيقة

بعد نشر إصلاح البيانات المنظمة، ابدأ Validate Fix مرة واحدة لتقريري Dataset وProfilePage، ثم انتظر بيانات الزحف الجديدة. لا تُعد إرسال Sitemap إلا لأن النسخة المنشورة تغيّرت، ولا تكرر طلبات الفهرسة الأربعة.

## ملفات أو بيانات محلية لا تنتقل عبر Git

- لا يوجد عمل مهم بقي محليًا فقط. ملف الحزمة الأصلي وسيلة نقل تاريخية وليس مصدر الحقيقة بعد دمج PR #65.

## فحص الاستلام على الجهاز الآخر

اقرأ `PROJECT_STATE.json` أولًا، وتأكد أن الحالة `deployed_monitoring` وأن PR #65 مدموج وأن Sitemap تسجل 52 صفحة مكتشفة. لا تبدأ أي تنفيذ تقني جديد قبل ظهور بيانات زحف جديدة من Search Console.
