# HANDOFF — الحالة الحية للموقع

آخر تحديث: 2026-09-23 13:33 Europe/Vienna

## الحالة الحالية
- المستودع: `Ahmed-Alhafiz/ahmed-alhafiz.github.io`، ومصدر الحقيقة هو GitHub.
- التغيير التنفيذي المنشور: `main` على SHA `b5adef24fc6b9817402c85f093f4392dcb031ff0` بعد دمج PR #61.
- Ruleset `Protect main` فعّال ويتطلب Pull Request وستة فحوص، بلا bypass.
- المهمة المنجزة: إنشاء مركز صحفي ثنائي اللغة، تقوية الربط الدلالي لهوية الكاتب، وإصلاح بطاقة المشاركة الاجتماعية التالفة.

## ما تم فعليًا
- بقي الدومين الرسمي `https://ahmedalhafiz.com` مرتبطًا بـGitHub Pages مع HTTPS وتحويل `www` والرابط القديم، وعملية **Change of Address** المقبولة في Google Search Console ما زالت أساس مرحلة الانتقال الحالية.
- بقي اسم الموقع الموحد وشعار القلم وحزمة favicon المنشورة كما هي من المرحلة السابقة.
- أضيفت صفحتا `https://ahmedalhafiz.com/press/` و`https://ahmedalhafiz.com/en/press/` بسيرة قابلة للاقتباس، بيانات هوية دقيقة، أصول إعلامية، حالة المؤلفات وروابط التحقق.
- رُبطت الصفحتان بكيان الكاتب الرسمي في `author.json` عبر `subjectOf` وعقدتي `WebPage` من دون تكرار عقدة `Person`.
- أضيفت روابط المركز الصحفي إلى صفحات الكاتب والتنقل العام، وأضيفت الصفحتان إلى `sitemap.xml`.
- ارتفع المخزون العام من 46 إلى 48 صفحة/عنوان canonical/رابط Sitemap.
- استُبدل `ahmed-alhafiz-social-card.png` التالف بملف PNG صالح بنسبة 1.91:1 وأبعاد 1733×907، يحافظ على صورة الكاتب وشعار القلم ولا يتضمن نصًا مولدًا.
- بقيت حالة جميع الكتب واضحة: قيد الإصدار وغير منشورة رسميًا، بلا ناشر أو ISBN أو مبيعات أو مراجعات غير مثبتة.
- لم تُنشأ حسابات أو مراجع خارجية زائفة، ولم تُضف ادعاءات سيرة غير موثقة.
- أصلح PR #61 قيمة `dateModified` في عقدة `ProfilePage` للصفحات العربية والإنجليزية والألمانية الست إلى DateTime بصيغة ISO 8601، وحدّث قيم `lastmod` المطابقة في `sitemap.xml` فقط؛ لم يتغير محتوى مرئي أو حقائق أو روابط.

## ما يجري العمل عليه الآن
- لا توجد تغييرات موقع تنفيذية مفتوحة. المطلوب الآن انتظار إعادة زحف Google لإصلاح `dateModified` ثم مراجعة Search Console فقط.

## التحقق المنجز
- اجتاز PR #59 الفحوص الستة الإلزامية ثم دُمج إلى `main`.
- نجحت جميع مهام `main`، ومنها Pages deployment وIndexNow والإبلاغ عن حالة البناء.
- نجحت محليًا فحوص Site وPresentation وEditorial وDiscovery وDossier وEntity وVisibility وUX وArabic UI وCitation/Research Architecture ذات الصلة وIndexNow dry-run و`git diff --check`.
- اجتاز الفحص البصري الآلي في CI الصفحتين الجديدتين وموضع الأصول عبر المقاسات الثلاثة، كما فُحصتا فعليًا في المتصفح على سطح المكتب وهاتف 390×844 بلا تمرير أفقي أو صور مكسورة.
- بعد النشر: عادت الصفحتان العربية والإنجليزية بالحالة 200، وحُمّلت الأصول الثلاثة بأبعادها الصحيحة.
- جميع روابط Sitemap الـ48 أعادت HTTP 200، ونجح فحص Visibility الحي بلا أخطاء.
- تطابق SHA-256 لبطاقة المشاركة المنشورة مع الملف المختبر محليًا، ونوعها `image/png` وحجمها 1,595,409 بايت.
- بقيت تحويلات `www` والرابط القديم إلى الدومين الأساسي مع حفظ المسار، وشهادة TLS تغطي `ahmedalhafiz.com` و`www.ahmedalhafiz.com`.
- سجل TXT الخاص بتحقق GitHub Pages ما زال موجودًا عند `_github-pages-challenge-Ahmed-Alhafiz.ahmedalhafiz.com`، كما بقي سجل تحقق Google.
- اجتاز PR #61 الفحوص الستة المطلوبة ودُمج عند `b5adef24fc6b9817402c85f093f4392dcb031ff0`. بعد نشر Pages، أعادت `/` و`/about/` و`/en/` و`/en/about/` و`/de/` و`/de/about/` الحالة 200، وكلها تعرض `ProfilePage.dateModified` بالقيمة `2026-09-23T11:49:49+02:00`.

## ما لم يُفحص / حدود الدليل
- لا يوجد ادعاء بضمان المركز الأول أو مدة محددة لظهور الصفحتين أو الاسم أو الأيقونة؛ ذلك يتوقف على إعادة زحف Google وإشارات مستقلة من الويب.
- لا تثبت الفحوص التقنية أن الصفحتين دخلتا فهرس Google بعد، ولا تثبت ترتيبًا أو Knowledge Panel أو استشهادات مستقلة.
- لم تُكرر طلبات URL Inspection لأن الطلب السابق للصفحة الرئيسية مقبول؛ خريطة الموقع الحية تعرض الآن 48 رابطًا ويمكن لمحركات البحث إعادة جلبها.
- واجهة GitHub Pages كانت تعرض سابقًا `DNS Check in Progress` رغم نجاح DNS وHTTPS والتحويلات فعليًا؛ لا تغيّر DNS عشوائيًا ما دامت الفحوص العامة ناجحة.
- تقرير ProfilePage في Search Console ما زال يمثل الحالة قبل إعادة الزحف: تنبيه `dateModified` لأربعة عناصر لا يُدّعى أنه اختفى بعد. خريطة الموقع ناجحة، وآخر رقم مؤكد فيها هو 46 صفحة مكتشفة؛ لا يوجد ادعاء بأن 48 صفحة فُهرست.

## المخاطر أو الـblockers
- لا توجد عوائق معروفة حاليًا.

## الخطوة التالية الدقيقة
1. راجع Search Console فقط بعد إعادة الزحف لتقييم تنبيه `dateModified` وحالة الفهرسة؛ لا تُعِد إرسال خريطة الموقع ولا تكرر طلب فهرسة الصفحة الرئيسية.

## ملفات أو بيانات محلية لا تنتقل عبر Git
- نسخة العمل: `C:\Users\xxx\Ahmed-Projects\work\site-seo-migration-2026-09-22`.

## فحص الاستلام على الجهاز الآخر
1. اقرأ HEAD البعيد قبل أي تعديل.
2. اقرأ `PROJECT_STATE.json` وملفات الحوكمة وهذا الملف.
3. لا تكرر فحصًا لم تتغير مدخلاته.
