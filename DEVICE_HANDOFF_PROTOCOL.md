# بروتوكول الانتقال بين الهاتف واللابتوب

هذا البروتوكول إلزامي عند الانتقال بين جهازين أو بين ChatGPT وCodex/Work. الهدف منع فقد التقدم، العمل من نسخة قديمة، أو تعديل الفرع نفسه من جهازين في الوقت نفسه.

## القاعدة العليا
GitHub هو المصدر الرسمي للحالة. لا تُعتبر المحادثة أو مجلد الجهاز أو لقطة شاشة مصدرًا أعلى من GitHub.

## قبل ترك الجهاز الحالي
1. افحص `git status` والفرع الحالي و`HEAD`.
2. راجع الـdiff كاملًا واستبعد build/cache/temp/secrets.
3. شغّل الفحوص المناسبة وفق `QUALITY_GATES.md`.
4. أنشئ commit واضحًا أو checkpoint آمنًا لكل العمل الحقيقي.
5. Push إلى GitHub دون force.
6. تحقق فعليًا أن SHA البعيد يطابق SHA المحلي.
7. حدّث `HANDOFF.md` و`PROJECT_STATE.json` إذا تغيّرت حالة العمل.
8. وثّق أي شيء بقي محليًا ولا ينتقل عبر Git.

لا تقل «تم التسليم» قبل اكتمال هذه الخطوات.

## عند الاستلام على الجهاز الآخر
1. لا تبدأ تعديلًا مباشرة.
2. اقرأ ملفات الحوكمة ثم `HANDOFF.md` و`PROJECT_STATE.json`.
3. نفّذ fetch/pull آمنًا بلا أوامر تنظيف مدمرة.
4. تحقق من الفرع وSHA البعيد قبل أي تعديل.
5. إذا كان الجهاز يحمل نسخة أقدم أو تغييرات محلية غير معروفة، توقف عن الكتابة وحل التعارض أولًا.
6. شغّل فحصًا سريعًا مناسبًا للتأكد أن نقطة الاستلام قابلة للعمل.

## منع التعديل المتزامن
- ممنوع أن يعمل الهاتف واللابتوب في الوقت نفسه على نفس الفرع مع تغييرات غير مرفوعة.
- عند الحاجة لعمل متوازٍ، استخدم فرعين مستقلين ثم ادمجهما عبر PR.
- لا تبدأ جلسة جديدة على فرع العمل إذا كانت جلسة أخرى لديها تغييرات محلية غير مرفوعة.

## قاعدة التوفير
- خلال التطوير اليومي استخدم commits على فرع العمل وفحوصًا محلية/مستهدفة.
- لا تفتح PR إلى `main` إلا عندما تصبح الدفعة جاهزة للمراجعة، لأن PR يشغّل CI أثقل.
- لا تؤجل فحصًا ضروريًا فقط لتوفير المال؛ التوفير يحدد توقيت الفحص لا معيار الجودة.

## معيار نجاح الانتقال
الانتقال ناجح فقط عندما يكون الفرع وSHA البعيد معروفين، والعمل المهم مرفوعًا، والفحوص المطلوبة مسجلة، وما بقي محليًا موثقًا، والجهاز المستلم بدأ من نفس الحالة أو حالة أحدث بشكل مقصود.
