# بروتوكول الانتقال والتنسيق بين الهاتف واللابتوب

هذا البروتوكول إلزامي عند الانتقال بين الهاتف واللابتوب أو بين ChatGPT وCodex/Work. الهدف منع فقد التقدم، منع تكرار الشغل، وتوفير رصيد اللابتوب **من دون نقل عمل يستطيع الهاتف إنجازه**.

## القاعدة العليا

GitHub هو المصدر الرسمي للحالة. لا تُعتبر المحادثة أو مجلد الجهاز أو لقطة شاشة مصدرًا أعلى من GitHub.

## الهاتف هو الافتراضي — اللابتوب استثناء لحاجة فعلية

قبل أي مرحلة، اسأل سؤالًا واحدًا: **هل يستطيع الهاتف إكمال هذه الخطوة بالمعيار المطلوب وبالأدوات المتاحة؟**

- إذا نعم: تُنفذ على الهاتف. لا تُؤجل للابتوب لمجرد أن اللابتوب أقوى أو أسرع أو أريح.
- إذا لا: تُسجل كخطوة **Laptop-only** مع السبب المحدد، وتؤجل للابتوب.
- إذا كانت المهمة مختلطة: الهاتف ينفذ كل الأجزاء الممكنة، ويؤجل فقط الجزء الذي يحتاج اللابتوب حصرًا.
- بعد أن ينهي اللابتوب الجزء Laptop-only، يعود المتبقي إلى الهاتف متى أصبح قابلًا للتنفيذ هناك.

لا يجوز تحويل مهمة كاملة إلى اللابتوب لأن «جودته أفضل عمومًا». التحويل يحتاج عائقًا محددًا: أداة غير متاحة، build/runtime محلي، ملف محلي، معالجة ملفات/أصول كبيرة، أداة سطح مكتب لازمة، أو Gate بصري/تقني/نشر لا يمكن إثباته على الهاتف.

## واجب الهاتف عند وجود جزء Laptop-only

الهاتف لا يتوقف مبكرًا. قبل التسليم يجب أن:

1. يكمل كل ما يمكن إنجازه مستقلًا على الهاتف.
2. يثبت ما أنجزه بالفحوص المناسبة المتاحة.
3. يعمل commit/push عندما يكون ذلك مناسبًا ويتحقق من remote SHA.
4. يحدث `HANDOFF.md` و`PROJECT_STATE.json` إذا تغيرت حالة العمل.
5. يسجل فقط النقطة أو النقاط التي تحتاج اللابتوب، مع سبب واضح وأداة/فحص مطلوب.

## واجب اللابتوب

عند استلام العمل:

1. يقرأ `LAPTOP_CREDIT_POLICY.md` وحزمة التسليم.
2. لا يعيد اكتشاف أو إعادة تنفيذ ما أنجزه الهاتف إذا بقي الدليل صالحًا.
3. ينفذ الجزء Laptop-only وما لا يمكن فصله عنه بأمان فقط.
4. يستخدم الفحوص المستهدفة أولًا ثم يوسع حسب المخاطر.
5. يعمل checkpoint آمن وcommit/push ويتحقق من remote SHA.
6. إذا أصبح باقي العمل قابلًا للهاتف، يعيده للهاتف بدل الاستمرار باستهلاك الرصيد.

## حزمة التسليم بين الجهازين

أي تحويل يجب أن يتضمن:

1. المستودع.
2. الفرع الحالي.
3. HEAD/SHA البعيد المتحقق منه.
4. الهدف المحدد للمرحلة التالية.
5. الملفات أو النطاق المتأثر.
6. ما تم فعليًا وما تم التحقق منه.
7. ما بقي غير متحقق.
8. السبب الدقيق الذي يجعل الخطوة Laptop-only إن وُجد.
9. أول خطوة عملية على الجهاز الآخر والفحوص المطلوبة.

لا تطلب من الجهاز الآخر إعادة العمل من الصفر إذا كانت هذه المعلومات معروفة.

## قبل ترك الجهاز الحالي

1. افحص الفرع و`HEAD` و`git status` عند توفر البيئة المحلية.
2. راجع الـdiff واستبعد build/cache/temp/secrets.
3. شغّل الفحوص المناسبة وفق `QUALITY_GATES.md`.
4. أنشئ commit/checkpoint آمنًا للعمل الحقيقي.
5. Push دون force وتحقق من remote SHA.
6. حدّث `HANDOFF.md` و`PROJECT_STATE.json` عند تغير الحالة أو وجود Laptop-only blocker.
7. وثّق أي شيء بقي محليًا ولا ينتقل عبر Git.

## عند الاستلام على الجهاز الآخر

- تحقق من الفرع وSHA البعيد قبل أي تعديل.
- اقرأ ملفات الحوكمة والحالة وحزمة التسليم.
- لا تكرر فحصًا ما زال صالحًا إذا لم تتغير مدخلاته.
- إذا كانت هناك تغييرات محلية غير معروفة أو نسخة قديمة، توقف وحل التعارض أولًا.

## منع العمل المتزامن والتكرار

- ممنوع أن يعمل الهاتف واللابتوب في الوقت نفسه على نفس الفرع مع تغييرات غير مرفوعة.
- ممنوع إعادة العمل فقط لأن الجهاز الجديد لم يقرأ التسليم.
- إذا احتجت عملًا متوازيًا، استخدم فرعين مستقلين ثم PR.

## معيار نجاح التنسيق

التنسيق ناجح فقط عندما:

- الهاتف أنجز كل ما يستطيع قبل طلب اللابتوب.
- كل خطوة مؤجلة للابتوب لها سبب تقني/عملي محدد، لا مجرد تفضيل عام.
- الفرع وSHA معروفان ومتحققان.
- العمل المهم مرفوع والفحوص مسجلة.
- لا يوجد تكرار بين الجهازين.
- بعد انتهاء الحاجة الخاصة باللابتوب يعود العمل للهاتف عندما يكون ذلك ممكنًا.